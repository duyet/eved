---
description: Look up one specific fact or page from a known source — a quick, single-source retrieval that doesn't need full research.
---

# Getting information

For a direct lookup, go straight to the best source instead of answering from memory:

- A detail about Duyet (a CV fact, a specific post, recent GitHub activity) → the
  **duyet** connection.
- A specific web page or fact → the **firecrawl** connection (`firecrawl_scrape` for a
  known URL, `firecrawl_search` to find one first).

Return just what was asked, with the source link. If the source doesn't have it, say so —
don't guess.
